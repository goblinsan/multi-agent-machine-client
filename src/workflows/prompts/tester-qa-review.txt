You are reviewing the implementation of task "{{task.title}}" for project {{project_name}}.

Task description:
{{{task.description}}}

{{#if plan_required_files.length}}
Plan-required files that must be covered by this change:
{{#each plan_required_files}}- {{this}}
{{/each}}
{{/if}}

## Ground truth: deterministic test execution

{{#if detected_test_command}}
Detected test command: `{{detected_test_command}}`
{{#if pre_qa_test_result}}
Machine-executed test results (authoritative — your verdict MUST agree with these):
{{json pre_qa_test_result}}
{{else}}
The test command was detected but produced no parseable result object.
{{/if}}
{{#if pre_qa_test_error}}
Test execution error output:
{{truncate pre_qa_test_error 4000}}
{{/if}}
{{else}}
No runnable test command was detected in this repository. Treat missing test
infrastructure as a failure per your system instructions.
{{/if}}

## Changed files

{{#each repo_changed_files}}- {{this}}
{{/each}}

## Diff under review (feature branch vs base)

```diff
{{truncate repo_diff_patch 24000}}
```

## Your job

You CANNOT execute commands. Base your verdict ONLY on the evidence above.

1. If machine-executed test results are present, they are the ground truth for
   pass/fail counts. Do not invent test results that contradict them.
2. Inspect the diff for signs of corrupted application: files truncated or
   replaced wholesale, duplicated blocks, orphaned braces/brackets, unrelated
   code deleted, imports removed that are still referenced. Any such sign is a
   SEVERE finding and status must be "fail".
3. Verify the diff plausibly implements the task description and touches the
   plan-required files listed above. Missing coverage is a SEVERE finding.
{{#if tdd_aware}}
4. TDD stage is "{{tdd_stage}}" — apply the TDD awareness rules from your
   system instructions when judging failing tests.
{{/if}}

Respond with JSON only, following the schema in your system instructions.
